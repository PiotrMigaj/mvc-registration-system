spring:
  datasource:
    username: 'root'
    password: 'root'
    url: 'jdbc:mysql://localhost/spring-security-mvc?serverTimezone=Europe/Warsaw&createDatabaseIfNotExist=true'
  jpa:
    hibernate:
      ddl-auto: 'update'
      show-sql: true
      format_sql: true
      use_sql_comments: true
    properties:
      hibernate:
        dialect: 'org.hibernate.dialect.MySQL8Dialect'

